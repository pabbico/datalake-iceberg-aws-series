# Workshop Scripts

`end_to_end_pipeline.py` — optional helper to orchestrate/verify each stage
of the pipeline (source DB check → crawler → Glue jobs → sample Athena
query). Supplementary to the manual, console-driven workshop steps — not
a replacement for them.

## Status
Stub only — not implemented.
