# Athena SQL Scripts

| Script | Purpose |
|---|---|
| `01_create_iceberg_table.sql` | Create an Iceberg table over Gold data |
| `02_time_travel_query.sql` | Query a historical snapshot |
| `03_schema_evolution_demo.sql` | Add a column without rewriting data |
| `04_bronze_silver_gold_comparison.sql` | Compare row counts across medallion layers |

## Status
Stubs only — to be filled in once Part 3 pipeline produces real tables.
